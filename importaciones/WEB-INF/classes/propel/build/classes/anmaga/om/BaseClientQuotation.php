<?php

require_once 'propel/om/BaseObject.php';

require_once 'propel/om/Persistent.php';


include_once 'propel/util/Criteria.php';

include_once 'anmaga/ClientQuotationPeer.php';

/**
 * Base class that represents a row from the 'import_clientQuotation' table.
 *
 * Cotizacion a Cliente
 *
 * This class was autogenerated by Propel on:
 *
 * Wed Feb  4 14:12:35 2009
 *
 * @package    anmaga.om
 */
abstract class BaseClientQuotation extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ClientQuotationPeer
	 */
	protected static $peer;


	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;


	/**
	 * The value for the createdat field.
	 * @var        int
	 */
	protected $createdat;


	/**
	 * The value for the userid field.
	 * @var        int
	 */
	protected $userid;


	/**
	 * The value for the status field.
	 * @var        int
	 */
	protected $status;


	/**
	 * The value for the timestampstatus field.
	 * @var        int
	 */
	protected $timestampstatus;

	/**
	 * @var        User
	 */
	protected $aUser;

	/**
	 * Collection to store aggregation of collClientQuotationItems.
	 * @var        array
	 */
	protected $collClientQuotationItems;

	/**
	 * The criteria used to select the current contents of collClientQuotationItems.
	 * @var        Criteria
	 */
	protected $lastClientQuotationItemCriteria = null;

	/**
	 * Collection to store aggregation of collSupplierQuotations.
	 * @var        array
	 */
	protected $collSupplierQuotations;

	/**
	 * The criteria used to select the current contents of collSupplierQuotations.
	 * @var        Criteria
	 */
	protected $lastSupplierQuotationCriteria = null;

	/**
	 * Collection to store aggregation of collClientPurchaseOrders.
	 * @var        array
	 */
	protected $collClientPurchaseOrders;

	/**
	 * The criteria used to select the current contents of collClientPurchaseOrders.
	 * @var        Criteria
	 */
	protected $lastClientPurchaseOrderCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Get the [id] column value.
	 * Id de cotizacion de cliente
	 * @return     int
	 */
	public function getId()
	{

		return $this->id;
	}

	/**
	 * Get the [optionally formatted] [createdat] column value.
	 * Creation date for
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the integer unix timestamp will be returned.
	 * @return     mixed Formatted date/time value as string or integer unix timestamp (if format is NULL).
	 * @throws     PropelException - if unable to convert the date/time to timestamp.
	 */
	public function getCreatedat($format = 'Y-m-d H:i:s')
	{

		if ($this->createdat === null || $this->createdat === '') {
			return null;
		} elseif (!is_int($this->createdat)) {
			// a non-timestamp value was set externally, so we convert it
			$ts = strtotime($this->createdat);
			if ($ts === -1 || $ts === false) { // in PHP 5.1 return value changes to FALSE
				throw new PropelException("Unable to parse value of [createdat] as date/time value: " . var_export($this->createdat, true));
			}
		} else {
			$ts = $this->createdat;
		}
		if ($format === null) {
			return $ts;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $ts);
		} else {
			return date($format, $ts);
		}
	}

	/**
	 * Get the [userid] column value.
	 * User
	 * @return     int
	 */
	public function getUserid()
	{

		return $this->userid;
	}

	/**
	 * Get the [status] column value.
	 * Status de Cotizacion
	 * @return     int
	 */
	public function getStatus()
	{

		return $this->status;
	}

	/**
	 * Get the [optionally formatted] [timestampstatus] column value.
	 * Fecha del ultimo cambio de status
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the integer unix timestamp will be returned.
	 * @return     mixed Formatted date/time value as string or integer unix timestamp (if format is NULL).
	 * @throws     PropelException - if unable to convert the date/time to timestamp.
	 */
	public function getTimestampstatus($format = 'Y-m-d H:i:s')
	{

		if ($this->timestampstatus === null || $this->timestampstatus === '') {
			return null;
		} elseif (!is_int($this->timestampstatus)) {
			// a non-timestamp value was set externally, so we convert it
			$ts = strtotime($this->timestampstatus);
			if ($ts === -1 || $ts === false) { // in PHP 5.1 return value changes to FALSE
				throw new PropelException("Unable to parse value of [timestampstatus] as date/time value: " . var_export($this->timestampstatus, true));
			}
		} else {
			$ts = $this->timestampstatus;
		}
		if ($format === null) {
			return $ts;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $ts);
		} else {
			return date($format, $ts);
		}
	}

	/**
	 * Set the value of [id] column.
	 * Id de cotizacion de cliente
	 * @param      int $v new value
	 * @return     void
	 */
	public function setId($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = ClientQuotationPeer::ID;
		}

	} // setId()

	/**
	 * Set the value of [createdat] column.
	 * Creation date for
	 * @param      int $v new value
	 * @return     void
	 */
	public function setCreatedat($v)
	{

		if ($v !== null && !is_int($v)) {
			$ts = strtotime($v);
			if ($ts === -1 || $ts === false) { // in PHP 5.1 return value changes to FALSE
				throw new PropelException("Unable to parse date/time value for [createdat] from input: " . var_export($v, true));
			}
		} else {
			$ts = $v;
		}
		if ($this->createdat !== $ts) {
			$this->createdat = $ts;
			$this->modifiedColumns[] = ClientQuotationPeer::CREATEDAT;
		}

	} // setCreatedat()

	/**
	 * Set the value of [userid] column.
	 * User
	 * @param      int $v new value
	 * @return     void
	 */
	public function setUserid($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->userid !== $v) {
			$this->userid = $v;
			$this->modifiedColumns[] = ClientQuotationPeer::USERID;
		}

		if ($this->aUser !== null && $this->aUser->getId() !== $v) {
			$this->aUser = null;
		}

	} // setUserid()

	/**
	 * Set the value of [status] column.
	 * Status de Cotizacion
	 * @param      int $v new value
	 * @return     void
	 */
	public function setStatus($v)
	{

		// Since the native PHP type for this column is integer,
		// we will cast the input value to an int (if it is not).
		if ($v !== null && !is_int($v) && is_numeric($v)) {
			$v = (int) $v;
		}

		if ($this->status !== $v) {
			$this->status = $v;
			$this->modifiedColumns[] = ClientQuotationPeer::STATUS;
		}

	} // setStatus()

	/**
	 * Set the value of [timestampstatus] column.
	 * Fecha del ultimo cambio de status
	 * @param      int $v new value
	 * @return     void
	 */
	public function setTimestampstatus($v)
	{

		if ($v !== null && !is_int($v)) {
			$ts = strtotime($v);
			if ($ts === -1 || $ts === false) { // in PHP 5.1 return value changes to FALSE
				throw new PropelException("Unable to parse date/time value for [timestampstatus] from input: " . var_export($v, true));
			}
		} else {
			$ts = $v;
		}
		if ($this->timestampstatus !== $ts) {
			$this->timestampstatus = $ts;
			$this->modifiedColumns[] = ClientQuotationPeer::TIMESTAMPSTATUS;
		}

	} // setTimestampstatus()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (1-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      ResultSet $rs The ResultSet class with cursor advanced to desired record pos.
	 * @param      int $startcol 1-based offset column which indicates which restultset column to start with.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate(ResultSet $rs, $startcol = 1)
	{
		try {

			$this->id = $rs->getInt($startcol + 0);

			$this->createdat = $rs->getTimestamp($startcol + 1, null);

			$this->userid = $rs->getInt($startcol + 2);

			$this->status = $rs->getInt($startcol + 3);

			$this->timestampstatus = $rs->getTimestamp($startcol + 4, null);

			$this->resetModified();

			$this->setNew(false);

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 5; // 5 = ClientQuotationPeer::NUM_COLUMNS - ClientQuotationPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating ClientQuotation object", $e);
		}
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      Connection $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete($con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ClientQuotationPeer::DATABASE_NAME);
		}

		try {
			$con->begin();
			ClientQuotationPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}

	/**
	 * Stores the object in the database.  If the object is new,
	 * it inserts it; otherwise an update is performed.  This method
	 * wraps the doSave() worker method in a transaction.
	 *
	 * @param      Connection $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save($con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ClientQuotationPeer::DATABASE_NAME);
		}

		try {
			$con->begin();
			$affectedRows = $this->doSave($con);
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}

	/**
	 * Stores the object in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      Connection $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave($con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;


			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUser !== null) {
				if ($this->aUser->isModified()) {
					$affectedRows += $this->aUser->save($con);
				}
				$this->setUser($this->aUser);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ClientQuotationPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += ClientQuotationPeer::doUpdate($this, $con);
				}
				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collClientQuotationItems !== null) {
				foreach($this->collClientQuotationItems as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSupplierQuotations !== null) {
				foreach($this->collSupplierQuotations as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collClientPurchaseOrders !== null) {
				foreach($this->collClientPurchaseOrders as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;
		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUser !== null) {
				if (!$this->aUser->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUser->getValidationFailures());
				}
			}


			if (($retval = ClientQuotationPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collClientQuotationItems !== null) {
					foreach($this->collClientQuotationItems as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSupplierQuotations !== null) {
					foreach($this->collSupplierQuotations as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collClientPurchaseOrders !== null) {
					foreach($this->collClientPurchaseOrders as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ClientQuotationPeer::DATABASE_NAME);

		if ($this->isColumnModified(ClientQuotationPeer::ID)) $criteria->add(ClientQuotationPeer::ID, $this->id);
		if ($this->isColumnModified(ClientQuotationPeer::CREATEDAT)) $criteria->add(ClientQuotationPeer::CREATEDAT, $this->createdat);
		if ($this->isColumnModified(ClientQuotationPeer::USERID)) $criteria->add(ClientQuotationPeer::USERID, $this->userid);
		if ($this->isColumnModified(ClientQuotationPeer::STATUS)) $criteria->add(ClientQuotationPeer::STATUS, $this->status);
		if ($this->isColumnModified(ClientQuotationPeer::TIMESTAMPSTATUS)) $criteria->add(ClientQuotationPeer::TIMESTAMPSTATUS, $this->timestampstatus);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ClientQuotationPeer::DATABASE_NAME);

		$criteria->add(ClientQuotationPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of ClientQuotation (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCreatedat($this->createdat);

		$copyObj->setUserid($this->userid);

		$copyObj->setStatus($this->status);

		$copyObj->setTimestampstatus($this->timestampstatus);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach($this->getClientQuotationItems() as $relObj) {
				$copyObj->addClientQuotationItem($relObj->copy($deepCopy));
			}

			foreach($this->getSupplierQuotations() as $relObj) {
				$copyObj->addSupplierQuotation($relObj->copy($deepCopy));
			}

			foreach($this->getClientPurchaseOrders() as $relObj) {
				$copyObj->addClientPurchaseOrder($relObj->copy($deepCopy));
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a pkey column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     ClientQuotation Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ClientQuotationPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ClientQuotationPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a User object.
	 *
	 * @param      User $v
	 * @return     void
	 * @throws     PropelException
	 */
	public function setUser($v)
	{


		if ($v === null) {
			$this->setUserid(NULL);
		} else {
			$this->setUserid($v->getId());
		}


		$this->aUser = $v;
	}


	/**
	 * Get the associated User object
	 *
	 * @param      Connection Optional Connection object.
	 * @return     User The associated User object.
	 * @throws     PropelException
	 */
	public function getUser($con = null)
	{
		// include the related Peer class
		include_once 'anmaga/om/BaseUserPeer.php';

		if ($this->aUser === null && ($this->userid !== null)) {

			$this->aUser = UserPeer::retrieveByPK($this->userid, $con);

			/* The following can be used instead of the line above to
			   guarantee the related object contains a reference
			   to this object, but this level of coupling
			   may be undesirable in many circumstances.
			   As it can lead to a db query with many results that may
			   never be used.
			   $obj = UserPeer::retrieveByPK($this->userid, $con);
			   $obj->addUsers($this);
			 */
		}
		return $this->aUser;
	}

	/**
	 * Temporary storage of collClientQuotationItems to save a possible db hit in
	 * the event objects are add to the collection, but the
	 * complete collection is never requested.
	 * @return     void
	 */
	public function initClientQuotationItems()
	{
		if ($this->collClientQuotationItems === null) {
			$this->collClientQuotationItems = array();
		}
	}

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this ClientQuotation has previously
	 * been saved, it will retrieve related ClientQuotationItems from storage.
	 * If this ClientQuotation is new, it will return
	 * an empty collection or the current collection, the criteria
	 * is ignored on a new object.
	 *
	 * @param      Connection $con
	 * @param      Criteria $criteria
	 * @throws     PropelException
	 */
	public function getClientQuotationItems($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/om/BaseClientQuotationItemPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collClientQuotationItems === null) {
			if ($this->isNew()) {
			   $this->collClientQuotationItems = array();
			} else {

				$criteria->add(ClientQuotationItemPeer::CLIENTQUOTATIONID, $this->getId());

				ClientQuotationItemPeer::addSelectColumns($criteria);
				$this->collClientQuotationItems = ClientQuotationItemPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ClientQuotationItemPeer::CLIENTQUOTATIONID, $this->getId());

				ClientQuotationItemPeer::addSelectColumns($criteria);
				if (!isset($this->lastClientQuotationItemCriteria) || !$this->lastClientQuotationItemCriteria->equals($criteria)) {
					$this->collClientQuotationItems = ClientQuotationItemPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastClientQuotationItemCriteria = $criteria;
		return $this->collClientQuotationItems;
	}

	/**
	 * Returns the number of related ClientQuotationItems.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      Connection $con
	 * @throws     PropelException
	 */
	public function countClientQuotationItems($criteria = null, $distinct = false, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/om/BaseClientQuotationItemPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		$criteria->add(ClientQuotationItemPeer::CLIENTQUOTATIONID, $this->getId());

		return ClientQuotationItemPeer::doCount($criteria, $distinct, $con);
	}

	/**
	 * Method called to associate a ClientQuotationItem object to this object
	 * through the ClientQuotationItem foreign key attribute
	 *
	 * @param      ClientQuotationItem $l ClientQuotationItem
	 * @return     void
	 * @throws     PropelException
	 */
	public function addClientQuotationItem(ClientQuotationItem $l)
	{
		$this->collClientQuotationItems[] = $l;
		$l->setClientQuotation($this);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this ClientQuotation is new, it will return
	 * an empty collection; or if this ClientQuotation has previously
	 * been saved, it will retrieve related ClientQuotationItems from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in ClientQuotation.
	 */
	public function getClientQuotationItemsJoinProduct($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/om/BaseClientQuotationItemPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collClientQuotationItems === null) {
			if ($this->isNew()) {
				$this->collClientQuotationItems = array();
			} else {

				$criteria->add(ClientQuotationItemPeer::CLIENTQUOTATIONID, $this->getId());

				$this->collClientQuotationItems = ClientQuotationItemPeer::doSelectJoinProduct($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ClientQuotationItemPeer::CLIENTQUOTATIONID, $this->getId());

			if (!isset($this->lastClientQuotationItemCriteria) || !$this->lastClientQuotationItemCriteria->equals($criteria)) {
				$this->collClientQuotationItems = ClientQuotationItemPeer::doSelectJoinProduct($criteria, $con);
			}
		}
		$this->lastClientQuotationItemCriteria = $criteria;

		return $this->collClientQuotationItems;
	}

	/**
	 * Temporary storage of collSupplierQuotations to save a possible db hit in
	 * the event objects are add to the collection, but the
	 * complete collection is never requested.
	 * @return     void
	 */
	public function initSupplierQuotations()
	{
		if ($this->collSupplierQuotations === null) {
			$this->collSupplierQuotations = array();
		}
	}

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this ClientQuotation has previously
	 * been saved, it will retrieve related SupplierQuotations from storage.
	 * If this ClientQuotation is new, it will return
	 * an empty collection or the current collection, the criteria
	 * is ignored on a new object.
	 *
	 * @param      Connection $con
	 * @param      Criteria $criteria
	 * @throws     PropelException
	 */
	public function getSupplierQuotations($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/om/BaseSupplierQuotationPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSupplierQuotations === null) {
			if ($this->isNew()) {
			   $this->collSupplierQuotations = array();
			} else {

				$criteria->add(SupplierQuotationPeer::CLIENTQUOTATIONID, $this->getId());

				SupplierQuotationPeer::addSelectColumns($criteria);
				$this->collSupplierQuotations = SupplierQuotationPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SupplierQuotationPeer::CLIENTQUOTATIONID, $this->getId());

				SupplierQuotationPeer::addSelectColumns($criteria);
				if (!isset($this->lastSupplierQuotationCriteria) || !$this->lastSupplierQuotationCriteria->equals($criteria)) {
					$this->collSupplierQuotations = SupplierQuotationPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSupplierQuotationCriteria = $criteria;
		return $this->collSupplierQuotations;
	}

	/**
	 * Returns the number of related SupplierQuotations.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      Connection $con
	 * @throws     PropelException
	 */
	public function countSupplierQuotations($criteria = null, $distinct = false, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/om/BaseSupplierQuotationPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		$criteria->add(SupplierQuotationPeer::CLIENTQUOTATIONID, $this->getId());

		return SupplierQuotationPeer::doCount($criteria, $distinct, $con);
	}

	/**
	 * Method called to associate a SupplierQuotation object to this object
	 * through the SupplierQuotation foreign key attribute
	 *
	 * @param      SupplierQuotation $l SupplierQuotation
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSupplierQuotation(SupplierQuotation $l)
	{
		$this->collSupplierQuotations[] = $l;
		$l->setClientQuotation($this);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this ClientQuotation is new, it will return
	 * an empty collection; or if this ClientQuotation has previously
	 * been saved, it will retrieve related SupplierQuotations from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in ClientQuotation.
	 */
	public function getSupplierQuotationsJoinSupplier($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/om/BaseSupplierQuotationPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSupplierQuotations === null) {
			if ($this->isNew()) {
				$this->collSupplierQuotations = array();
			} else {

				$criteria->add(SupplierQuotationPeer::CLIENTQUOTATIONID, $this->getId());

				$this->collSupplierQuotations = SupplierQuotationPeer::doSelectJoinSupplier($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SupplierQuotationPeer::CLIENTQUOTATIONID, $this->getId());

			if (!isset($this->lastSupplierQuotationCriteria) || !$this->lastSupplierQuotationCriteria->equals($criteria)) {
				$this->collSupplierQuotations = SupplierQuotationPeer::doSelectJoinSupplier($criteria, $con);
			}
		}
		$this->lastSupplierQuotationCriteria = $criteria;

		return $this->collSupplierQuotations;
	}

	/**
	 * Temporary storage of collClientPurchaseOrders to save a possible db hit in
	 * the event objects are add to the collection, but the
	 * complete collection is never requested.
	 * @return     void
	 */
	public function initClientPurchaseOrders()
	{
		if ($this->collClientPurchaseOrders === null) {
			$this->collClientPurchaseOrders = array();
		}
	}

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this ClientQuotation has previously
	 * been saved, it will retrieve related ClientPurchaseOrders from storage.
	 * If this ClientQuotation is new, it will return
	 * an empty collection or the current collection, the criteria
	 * is ignored on a new object.
	 *
	 * @param      Connection $con
	 * @param      Criteria $criteria
	 * @throws     PropelException
	 */
	public function getClientPurchaseOrders($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/om/BaseClientPurchaseOrderPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collClientPurchaseOrders === null) {
			if ($this->isNew()) {
			   $this->collClientPurchaseOrders = array();
			} else {

				$criteria->add(ClientPurchaseOrderPeer::CLIENTQUOTATIONID, $this->getId());

				ClientPurchaseOrderPeer::addSelectColumns($criteria);
				$this->collClientPurchaseOrders = ClientPurchaseOrderPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ClientPurchaseOrderPeer::CLIENTQUOTATIONID, $this->getId());

				ClientPurchaseOrderPeer::addSelectColumns($criteria);
				if (!isset($this->lastClientPurchaseOrderCriteria) || !$this->lastClientPurchaseOrderCriteria->equals($criteria)) {
					$this->collClientPurchaseOrders = ClientPurchaseOrderPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastClientPurchaseOrderCriteria = $criteria;
		return $this->collClientPurchaseOrders;
	}

	/**
	 * Returns the number of related ClientPurchaseOrders.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      Connection $con
	 * @throws     PropelException
	 */
	public function countClientPurchaseOrders($criteria = null, $distinct = false, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/om/BaseClientPurchaseOrderPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		$criteria->add(ClientPurchaseOrderPeer::CLIENTQUOTATIONID, $this->getId());

		return ClientPurchaseOrderPeer::doCount($criteria, $distinct, $con);
	}

	/**
	 * Method called to associate a ClientPurchaseOrder object to this object
	 * through the ClientPurchaseOrder foreign key attribute
	 *
	 * @param      ClientPurchaseOrder $l ClientPurchaseOrder
	 * @return     void
	 * @throws     PropelException
	 */
	public function addClientPurchaseOrder(ClientPurchaseOrder $l)
	{
		$this->collClientPurchaseOrders[] = $l;
		$l->setClientQuotation($this);
	}

} // BaseClientQuotation
