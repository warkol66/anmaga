<?php 

require_once 'propel/om/BaseObject.php';

 
require_once 'propel/om/Persistent.php';

 

include_once 'propel/util/Criteria.php';

include_once 'anmaga/CategoryPeer.php';

/**
 * Base class that represents a row from the 'category' table.
 *
 * Categorias 
 *
 * This class was autogenerated by Propel on:
 *
 * [05/14/07 17:23:25]
 *
 * You should not use this class directly.  It should not even be
 * extended; all references should be to Category class. 
 * 
 * @package anmaga 
 */
abstract class BaseCategory extends BaseObject implements Persistent {
	
	/** 
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var CategoryPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var int	  
	 */
	protected $id;	

	/**
	 * The value for the name field.
	 * @var string	  
	 */
	protected $name;	

	/**
	 * The value for the active field.
	 * @var boolean	  
	 */
	protected $active;	
  
	/**
	 * Get the Id column value.
	 *  
	 * @return int	  
	 */
	public function getId()
	{
		return $this->id;
	}


	/**
	 * Set the value of `id` column.	  
	 *  
	 * @param int $v new value
	 * @return void
	 * @throws PropelException 
	 */
	public function setId($v)
	{
		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = CategoryPeer::ID;
		}
			
		  // update associated GroupCategory		  
		  if ($this->collGroupCategorys !== null) {
			  for ($i=0,$size=count($this->collGroupCategorys); $i < $size; $i++) {
				  $this->collGroupCategorys[$i]->setCategoryid($v);
			  }
		  }
			  	
		  // update associated UsersByAffiliateGroupCategory		  
		  if ($this->collUsersByAffiliateGroupCategorys !== null) {
			  for ($i=0,$size=count($this->collUsersByAffiliateGroupCategorys); $i < $size; $i++) {
				  $this->collUsersByAffiliateGroupCategorys[$i]->setCategoryid($v);
			  }
		  }
			  		
	}

  
	/**
	 * Get the Name column value.
	 * Category name 
	 * @return string	  
	 */
	public function getName()
	{
		return $this->name;
	}


	/**
	 * Set the value of `name` column.	  
	 * Category name 
	 * @param string $v new value
	 * @return void
	 *  
	 */
	public function setName($v)
	{
		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = CategoryPeer::NAME;
		}
				
	}

  
	/**
	 * Get the Active column value.
	 * Is category active? 
	 * @return boolean	  
	 */
	public function getActive()
	{
		return $this->active;
	}


	/**
	 * Set the value of `active` column.	  
	 * Is category active? 
	 * @param boolean $v new value
	 * @return void
	 *  
	 */
	public function setActive($v)
	{
		if ($this->active !== $v) {
			$this->active = $v;
			$this->modifiedColumns[] = CategoryPeer::ACTIVE;
		}
				
	}


	
	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (1-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param ResultSet $rs The ResultSet class with cursor advanced to desired record pos.
	 * @param int $startcol 1-based offset column which indicates which restultset column to start with.
	 * @return void
	 * @throws PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate(ResultSet $rs, $startcol = 1)
	{
		try {
			$this->id = $rs->getInt($startcol + 0);					
			$this->name = $rs->getString($startcol + 1);					
			$this->active = $rs->getBoolean($startcol + 2);					
			 
			$this->resetModified();
			$this->setNew(false);
		
		} catch (Exception $e) {
			throw new PropelException("Error populating Category object", $e);
		}		
	
	}
	
	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 * 
	 * @return Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{		
		$criteria = new Criteria(CategoryPeer::DATABASE_NAME);
		$criteria->add(CategoryPeer::ID, $this->id);
		return $criteria;			
	}
	
	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{		
		$criteria = new Criteria(CategoryPeer::DATABASE_NAME);
		if ($this->isColumnModified(CategoryPeer::ID)) $criteria->add(CategoryPeer::ID, $this->id);
		if ($this->isColumnModified(CategoryPeer::NAME)) $criteria->add(CategoryPeer::NAME, $this->name);
		if ($this->isColumnModified(CategoryPeer::ACTIVE)) $criteria->add(CategoryPeer::ACTIVE, $this->active);
		return $criteria;			
	}
	
	

	/**
	 * Collection to store aggregation of collGroupCategorys	  
	 * @var array
	 */
	protected $collGroupCategorys; 

	/**
	 * Temporary storage of collGroupCategorys to save a possible db hit in
	 * the event objects are add to the collection, but the
	 * complete collection is never requested.
	 * @return void
	 */
	public function initGroupCategorys()
	{
		if ($this->collGroupCategorys === null) {
			$this->collGroupCategorys = array();
		}
	}

	/**
	 * Method called to associate a GroupCategory object to this object
	 * through the GroupCategory foreign key attribute
	 *
	 * @param GroupCategory $l $className
	 * @return void
	 * @throws PropelException
	 */
	public function addGroupCategory(GroupCategory $l)
	{
		$this->collGroupCategorys[] = $l;
		$l->setCategory($this);
	}

	/**
	 * The criteria used to select the current contents of collGroupCategorys.
	 * @var Criteria
	 */
	private $lastGroupCategorysCriteria = null;

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Category has previously
	 * been saved, it will retrieve related GroupCategorys from storage.
	 * If this Category is new, it will return
	 * an empty collection or the current collection, the criteria
	 * is ignored on a new object.
	 *
	 * @param Connection $con
	 * @param Criteria $criteria
	 * @throws PropelException
	 */
	public function getGroupCategorys($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/GroupCategoryPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
				
		if ($this->collGroupCategorys === null) {
			if ($this->isNew()) {
			   $this->collGroupCategorys = array();
			} else {
	
				$criteria->add(GroupCategoryPeer::CATEGORYID, $this->getId() );

				$this->collGroupCategorys = GroupCategoryPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.

				$criteria->add(GroupCategoryPeer::CATEGORYID, $this->getId());

				if (!isset($this->lastGroupCategorysCriteria) || !$this->lastGroupCategorysCriteria->equals($criteria)) {
					$this->collGroupCategorys = GroupCategoryPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastGroupCategorysCriteria = $criteria;

		return $this->collGroupCategorys;
	}
	

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Category is new, it will return
	 * an empty collection; or if this Category has previously
	 * been saved, it will retrieve related GroupCategorys from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Category.
	 */
	public function getGroupCategorysJoinGroup($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/GroupCategoryPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		
		if ($this->collGroupCategorys === null) {
			if ($this->isNew()) {
			   $this->collGroupCategorys = array();
			} else {
	   
				$criteria->add(GroupCategoryPeer::CATEGORYID, $this->getId());
				$this->collGroupCategorys = GroupCategoryPeer::doSelectJoinGroup($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.
	  
			$criteria->add(GroupCategoryPeer::CATEGORYID, $this->getId());
			if (!isset($this->lastGroupCategorysCriteria) || !$this->lastGroupCategorysCriteria->equals($criteria)) {
				$this->collGroupCategorys = GroupCategoryPeer::doSelectJoinGroup($criteria, $con);
			}
		}
		$this->lastGroupCategorysCriteria = $criteria;

		return $this->collGroupCategorys;
	}

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Category is new, it will return
	 * an empty collection; or if this Category has previously
	 * been saved, it will retrieve related GroupCategorys from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Category.
	 */
	public function getGroupCategorysJoinCategory($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/GroupCategoryPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		
		if ($this->collGroupCategorys === null) {
			if ($this->isNew()) {
			   $this->collGroupCategorys = array();
			} else {
	   
				$criteria->add(GroupCategoryPeer::CATEGORYID, $this->getId());
				$this->collGroupCategorys = GroupCategoryPeer::doSelectJoinCategory($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.
	  
			$criteria->add(GroupCategoryPeer::CATEGORYID, $this->getId());
			if (!isset($this->lastGroupCategorysCriteria) || !$this->lastGroupCategorysCriteria->equals($criteria)) {
				$this->collGroupCategorys = GroupCategoryPeer::doSelectJoinCategory($criteria, $con);
			}
		}
		$this->lastGroupCategorysCriteria = $criteria;

		return $this->collGroupCategorys;
	}

	/**
	 * Collection to store aggregation of collUsersByAffiliateGroupCategorys	  
	 * @var array
	 */
	protected $collUsersByAffiliateGroupCategorys; 

	/**
	 * Temporary storage of collUsersByAffiliateGroupCategorys to save a possible db hit in
	 * the event objects are add to the collection, but the
	 * complete collection is never requested.
	 * @return void
	 */
	public function initUsersByAffiliateGroupCategorys()
	{
		if ($this->collUsersByAffiliateGroupCategorys === null) {
			$this->collUsersByAffiliateGroupCategorys = array();
		}
	}

	/**
	 * Method called to associate a UsersByAffiliateGroupCategory object to this object
	 * through the UsersByAffiliateGroupCategory foreign key attribute
	 *
	 * @param UsersByAffiliateGroupCategory $l $className
	 * @return void
	 * @throws PropelException
	 */
	public function addUsersByAffiliateGroupCategory(UsersByAffiliateGroupCategory $l)
	{
		$this->collUsersByAffiliateGroupCategorys[] = $l;
		$l->setCategory($this);
	}

	/**
	 * The criteria used to select the current contents of collUsersByAffiliateGroupCategorys.
	 * @var Criteria
	 */
	private $lastUsersByAffiliateGroupCategorysCriteria = null;

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Category has previously
	 * been saved, it will retrieve related UsersByAffiliateGroupCategorys from storage.
	 * If this Category is new, it will return
	 * an empty collection or the current collection, the criteria
	 * is ignored on a new object.
	 *
	 * @param Connection $con
	 * @param Criteria $criteria
	 * @throws PropelException
	 */
	public function getUsersByAffiliateGroupCategorys($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/UsersByAffiliateGroupCategoryPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
				
		if ($this->collUsersByAffiliateGroupCategorys === null) {
			if ($this->isNew()) {
			   $this->collUsersByAffiliateGroupCategorys = array();
			} else {
	
				$criteria->add(UsersByAffiliateGroupCategoryPeer::CATEGORYID, $this->getId() );

				$this->collUsersByAffiliateGroupCategorys = UsersByAffiliateGroupCategoryPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.

				$criteria->add(UsersByAffiliateGroupCategoryPeer::CATEGORYID, $this->getId());

				if (!isset($this->lastUsersByAffiliateGroupCategorysCriteria) || !$this->lastUsersByAffiliateGroupCategorysCriteria->equals($criteria)) {
					$this->collUsersByAffiliateGroupCategorys = UsersByAffiliateGroupCategoryPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastUsersByAffiliateGroupCategorysCriteria = $criteria;

		return $this->collUsersByAffiliateGroupCategorys;
	}
	

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Category is new, it will return
	 * an empty collection; or if this Category has previously
	 * been saved, it will retrieve related UsersByAffiliateGroupCategorys from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Category.
	 */
	public function getUsersByAffiliateGroupCategorysJoinUsersByAffiliateGroup($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/UsersByAffiliateGroupCategoryPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		
		if ($this->collUsersByAffiliateGroupCategorys === null) {
			if ($this->isNew()) {
			   $this->collUsersByAffiliateGroupCategorys = array();
			} else {
	   
				$criteria->add(UsersByAffiliateGroupCategoryPeer::CATEGORYID, $this->getId());
				$this->collUsersByAffiliateGroupCategorys = UsersByAffiliateGroupCategoryPeer::doSelectJoinUsersByAffiliateGroup($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.
	  
			$criteria->add(UsersByAffiliateGroupCategoryPeer::CATEGORYID, $this->getId());
			if (!isset($this->lastUsersByAffiliateGroupCategorysCriteria) || !$this->lastUsersByAffiliateGroupCategorysCriteria->equals($criteria)) {
				$this->collUsersByAffiliateGroupCategorys = UsersByAffiliateGroupCategoryPeer::doSelectJoinUsersByAffiliateGroup($criteria, $con);
			}
		}
		$this->lastUsersByAffiliateGroupCategorysCriteria = $criteria;

		return $this->collUsersByAffiliateGroupCategorys;
	}

	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Category is new, it will return
	 * an empty collection; or if this Category has previously
	 * been saved, it will retrieve related UsersByAffiliateGroupCategorys from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Category.
	 */
	public function getUsersByAffiliateGroupCategorysJoinCategory($criteria = null, $con = null)
	{
		// include the Peer class
		include_once 'anmaga/UsersByAffiliateGroupCategoryPeer.php';
		if ($criteria === null) {
			$criteria = new Criteria();
		}
		
		if ($this->collUsersByAffiliateGroupCategorys === null) {
			if ($this->isNew()) {
			   $this->collUsersByAffiliateGroupCategorys = array();
			} else {
	   
				$criteria->add(UsersByAffiliateGroupCategoryPeer::CATEGORYID, $this->getId());
				$this->collUsersByAffiliateGroupCategorys = UsersByAffiliateGroupCategoryPeer::doSelectJoinCategory($criteria, $con);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.
	  
			$criteria->add(UsersByAffiliateGroupCategoryPeer::CATEGORYID, $this->getId());
			if (!isset($this->lastUsersByAffiliateGroupCategorysCriteria) || !$this->lastUsersByAffiliateGroupCategorysCriteria->equals($criteria)) {
				$this->collUsersByAffiliateGroupCategorys = UsersByAffiliateGroupCategoryPeer::doSelectJoinCategory($criteria, $con);
			}
		}
		$this->lastUsersByAffiliateGroupCategorysCriteria = $criteria;

		return $this->collUsersByAffiliateGroupCategorys;
	}



 
	/**
	 * Removes this object from datastore and sets delete attribute.
	 * 
	 * @param Connection $con
	 * @return void
	 * @throws PropelException
	 * @see BaseObject::setDeleted()
	 * @see BaseObject::isDeleted()
	 */
	public function delete($con = null)
	{	
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}
		
		if ($con === null)
			$con = Propel::getConnection(CategoryPeer::DATABASE_NAME);

		try {
			$con->begin();
			CategoryPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}
	

	/**
	 * flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var boolean
	 */
	private $alreadyInSave = false;

	/**
	 * Stores the object in the database.  If the object is new,
	 * it inserts it; otherwise an update is performed.  This method
	 * wraps the doSave() worker method in a transaction.
	 *
	 * @param Connection $con
	 * @return void
	 * @throws PropelException
	 */
	public function save($con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}
		
		if ($con === null)
			$con = Propel::getConnection(CategoryPeer::DATABASE_NAME);

		try {
			$con->begin();
			$this->doSave($con);			
			$con->commit();
		} catch (PropelException $e) {
			$con->rollback();
			throw $e;
		}
	}
  
	/**
	 * Stores the object in the database.  If the object is new,
	 * it inserts it; otherwise an update is performed.
	 *
	 * @param Connection $con
	 * @return void
	 * @throws PropelException
	 */
		protected function doSave($con)	{ 
		
	 
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = CategoryPeer::doInsert($this, $con);
					$this->setId( $pk );  //[IMV] update autoincrement primary key
					$this->setNew(false);
				} else {
					CategoryPeer::doUpdate($this, $con);
				}			
				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}
			if ($this->collGroupCategorys !== null) {
				for ($i=0,$size=count($this->collGroupCategorys); $i < $size; $i++) {
					$this->collGroupCategorys[$i]->save($con);
				}
			  }
			if ($this->collUsersByAffiliateGroupCategorys !== null) {
				for ($i=0,$size=count($this->collUsersByAffiliateGroupCategorys); $i < $size; $i++) {
					$this->collUsersByAffiliateGroupCategorys[$i]->save($con);
				}
			  }

			$this->alreadyInSave = false;
		}
	}

	/**
	 * Validates the objects modified field values.
	 * This includes all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param mixed $columns Column name or an array of column names.
	 *
	 * @return mixed <code>true</code> if all columns pass validation
	 *			  or an array of <code>ValidationFailed</code> objects for columns that fail.
	 */
	public function validate($columns = null)
	{
	  if ($columns)
	  {
		return CategoryPeer::doValidate($this, $columns);
	  }

		return $this->doValidate();
	}

	/**
	 * flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var boolean
	 */
	protected $alreadyInValidation = false;
  
	/**
	 * This function performs the validation work for complex object models.
	 * 
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate()
	{
		if (! $this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;
	  
			$failureMap = array();	  

			if (($retval = CategoryPeer::doValidate($this)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}
	  
			if ($this->collGroupCategorys !== null) {
				for ($i=0,$size=count($this->collGroupCategorys); $i < $size; $i++) {
					if (($retval = $this->collGroupCategorys[$i]->validate()) !== true) {
						$failureMap = array_merge($failureMap, $retval);
					}
				}
			}
			if ($this->collUsersByAffiliateGroupCategorys !== null) {
				for ($i=0,$size=count($this->collUsersByAffiliateGroupCategorys); $i < $size; $i++) {
					if (($retval = $this->collUsersByAffiliateGroupCategorys[$i]->validate()) !== true) {
						$failureMap = array_merge($failureMap, $retval);
					}
				}
			}
	  
			$this->alreadyInValidation = false;
		}
	
		return (!empty($failureMap) ? $failureMap : true);
	}
  

	/**
	 * Set the PrimaryKey.
	 *
	 * @param mixed id Primary key.
	 * @return void
	 * @throws PropelException	 */
	public function setPrimaryKey($key) 
	{		
		$this->setId($key);
	}
	

	/**
	 * Returns an id that differentiates this object from others
	 * of its class.
	 * @return int 
	 */
	public function getPrimaryKey()
	{

		return $this->getId();

	}

	
	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *  
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 *  
	 * @return Category Clone of current object.
	 * @throws PropelException
	 */
	public function copy($deepCopy = false) 
	{
		$copyObj = new Category();
		$copyObj->setName($this->name);
		$copyObj->setActive($this->active);

		if ($deepCopy) {		
			// important: setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach($this->getGroupCategorys() as $relObj) {
				$copyObj->addGroupCategory($relObj->copy());
			}

			foreach($this->getUsersByAffiliateGroupCategorys() as $relObj) {
				$copyObj->addUsersByAffiliateGroupCategory($relObj->copy());
			}
		} // if ($deepCopy)
		
		$copyObj->setNew(true);
		
		$copyObj->setId(NULL); // this is a pkey column, so set to default value

		return $copyObj;
	}
		

	/**
	 * returns a peer instance associated with this om.  Since Peer classes	
	 * are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 * @return CategoryPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new CategoryPeer();
		}
		return self::$peer;
	}

}
